#include<stdio.h>
#include<stdlib.h>
struct node{
		int data;
		struct node*next;
	}head;
int main(){
	struct node*temp;
	struct node*head=NULL;
struct node*newnode;
for(i=0;i<n;i++){

newnode=(struct node*)malloc(sizeof(struct node))};
newnode->data=data;
newnode->next=head;
head=newnode;

temp=head;
while(temp!=NULL){
	printf("%d",temp->data);
	temp=temp->next;
}}
